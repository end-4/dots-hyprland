# exec = export SLURP_ARGS='-d -c DDE3EABB -b 41474D44 -s 00000000'

general {
    # col.active_border = rgba(E4E2E339)
    col.active_border = rgba(E5CCC3BE) rgba(6047DFBE) 45deg
    col.inactive_border = rgba(91909130)
}

misc {
    background_color = rgba(131314FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(131314FF)
        col.text = rgba(E4E2E3FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(E4E2E3), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(E4E2E3), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(E4E2E3), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(BCC8D3AA) rgba(BCC8D377),pinned:1
