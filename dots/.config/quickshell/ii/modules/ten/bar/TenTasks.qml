import QtQuick
import QtQuick.Layouts
import qs
import qs.services
import qs.modules.common
import qs.modules.ten.looks

Rectangle {
    id: root

    Layout.fillHeight: true
    Layout.fillWidth: true
    color: "transparent"
    implicitWidth: parent.width
    implicitHeight: 40

    // Windows 10 taskbar buttons would go here
    // Simplified placeholder showing the Windows 10 center-aligned taskbar style
}
