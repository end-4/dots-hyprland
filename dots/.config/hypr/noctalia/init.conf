# Example second desktop profile. In hyprland.conf set $HYPR_DESKTOP_SHELL = noctalia.
# Default: reuse the end-4 pack (submap lives in hyprland.conf; $qsConfig + modules from ii).
source = ../ii/init.conf
