# This file sources other files in `illogical-impulse` and `custom` folders
# You wanna add your stuff in files in `custom`

# Defaults
source= illogical-impulse/hypridle.conf

# Custom
source = custom/hypridle.conf
