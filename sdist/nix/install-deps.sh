# This script is meant to be sourced.
# It's not for directly running.

# This file is currently WIP.
printf "[$0]: The repository has flake for Nix with Home-manager.

In path 'sdist/nix/flake.nix'

Search for 'FIXME' in flake.nix
and replace username to your current user.

Then you can run 'home-manager switch --flake $HOME/path/to/dotfiles/sdist/nix#your-username'
"
