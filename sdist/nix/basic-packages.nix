# Basic packages
{ pkgs }:

{
  # Basic packages
  axel = pkgs.axel;
  bc = pkgs.bc;
  coreutils = pkgs.coreutils;
  cliphist = pkgs.cliphist;
  cmake = pkgs.cmake;
  curl = pkgs.curl;
  rsync = pkgs.rsync;
  wget = pkgs.wget;
  ripgrep = pkgs.ripgrep;
  jq = pkgs.jq;
  meson = pkgs.meson;
  xdg-user-dirs = pkgs.xdg-user-dirs;
}
